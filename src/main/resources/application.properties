# Default profile configuration
# Use 'dev' profile for local development (H2 database)
# Use 'prod' profile for production (PostgreSQL)

spring.profiles.active=dev

# Application name
spring.application.name=ToDoList REST API

